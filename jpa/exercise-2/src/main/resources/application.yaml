spring:
  application:
    name: exercise-2
  datasource:
    url: jdbc:postgresql://localhost:5432/jpa-exercise-2
    password: test
    username: admin
  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 8081
  error:
    include-errors: always