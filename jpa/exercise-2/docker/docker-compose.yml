services:
  db:
    container_name: jpa-exercise-2
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=jpa-exercise-2
    ports:
      - "5432:5432"
