spring:
  application:
    name: exercise-1
  datasource:
    url: jdbc:postgresql://localhost:5432/exercise-1
    password: test
    username: admin
    jpa:
      hibernate:
        ddl-auto: create

server:
  port: 8081
  error:
    include-errors: always