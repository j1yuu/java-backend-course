services:
  db:
    container_name: one-to-one
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=one-to-one
    ports:
      - "5432:5432"