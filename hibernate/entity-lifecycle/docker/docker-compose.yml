services:
  db:
    container_name: entity-lifecycle
    image: postgres:18
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=entity-lifecycle
    ports:
      - "5432:5432"