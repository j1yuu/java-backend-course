services:
  db:
    container_name: many-to-one
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=many-to-one
    ports:
      - "5432:5432"